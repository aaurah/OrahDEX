What you’ve effectively achieved

On-chain reality now matches the mythic spec:

• Settlement v2 OP_RETURN:• Includes HTLC commitment:• H:<secretHash>
• P:<htlcAddress>

• This turns every cross-chain fill into a verifiable, on-chain swap contract anchor instead of a “fake coinbase” placeholder.

• Real P2SH HTLC output in bsvBroadcaster.ts:• Output ordering:• 0: OP_RETURN (trade + HTLC commitment)
• 1: P2SH HTLC (cross-chain only)
• 2: P2PKH change

• Change math adjusted to subtract HTLC satoshis—no ghost value, no hidden float.

• Auto HTLC generation in orders.ts:• Cross-chain detection:• buyerNetwork !== sellerNetwork && !isBot

• Live chain-aware locktime:• locktimeBlocks = currentHeight + 144 (~24h)

• HTLC build pipeline:• buildHtlc() → buildP2SHLockingScript() → pass htlcP2SHScriptHex + htlcSatoshis into:• buildSettlement() (for v2 OP_RETURN)
• broadcastSettlement() (for actual P2SH output)


• API response now carries:• settlement: { type, crossChain, htlcAddress, htlcSecretHash, htlcLocktimeBlocks }


• Trade engine + UI now speak BSV UTXO fluently:• TradeResult extended with settlement object.
• Lifecycle documented:
lock → match → HTLC → P2SH broadcast → relayer claim / CLTV refund
• OrderForm.tsx + SettlementBanner:• State extended with crossChain, htlcAddress, settlementType, etc.
• Cross-chain trades visually marked with “Cross-Chain HTLC Settlement” + truncated P2SH address.




You’ve effectively moved from “fake settlement narrative” to real, inspectable, UTXO-scripted settlement.

---

Places I’d pressure-test next

If you want this to be mythically bulletproof, here’s where I’d go now:

1. HTLC parameter invariants• Check:• htlcSatoshis never exceeds input minus fees.
• locktimeBlocks is always ≥ currentHeight + safety margin.

• Edge case: very small trades where HTLC output might fall below dust.

2. Refund and claim paths• You’ve anchored the locking side.
• Make sure the unlocking story is equally explicit:• Relayer claim flow: spends P2SH with preimage.
• Timeout refund flow: CLTV path back to original owner.

• Even if implemented elsewhere, I’d:• Document the exact script branches in htlc.ts.
• Cross-link them from the trade lifecycle docs in tradeEngine.ts.


3. Cross-chain-only guarantees• Confirm:• HTLC generation is never triggered for same-chain trades.
• Bots are correctly excluded where intended (!isBot).

• Maybe add a small assert/log when HTLC is built:• if (!crossChain) throw (or at least warn in dev).


4. Indexing and observability• Now that HTLCs are real:• Do you have a chain monitor or indexer watching:• HTLC P2SH addresses
• Secret revelation
• Refund vs claim outcomes?


• If not, a minimal watcher that:• Subscribes to P2SH script hash
• Emits events into your internal bus
• Feeds a “Settlement Timeline” in the UI
would close the loop beautifully.


5. Replay and simulation• I’d run a scripted scenario set:• Case A: same-chain trade → no HTLC, v2 OP_RETURN still valid.
• Case B: cross-chain, relayer claims before timeout.
• Case C: cross-chain, timeout hits → refund path.

• For each:• Inspect raw tx:• Outputs, OP_RETURN, P2SH script.

• Confirm UI shows the correct settlement narrative.




---

One more narrative layer you might like

You’ve now got enough structure to name this as a “BSV Settlement v2 Covenant” in your docs:

• Layer 1: Trade match (off-chain coordination, on-chain truth)
• Layer 2: Settlement v2 (OP_RETURN + HTLC commitment)
• Layer 3: HTLC UTXO (P2SH script as the actual covenant)
• Layer 4: Chain monitor + relayer (keepers who witness and act)


If you want, next step we can:

• Draft a short, precise BSV Settlement v2 spec (dev-facing).
• Or design a “Settlement Explorer” UI section that surfaces:• OP_RETURN v2 payload
• HTLC address
• Current status: LOCKED | CLAIMED | REFUNDED | EXPIRED.