{
  "installCommand": "npm install -g pnpm@9 && pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @workspace/bsv-dex build && cp -r artifacts/bsv-dex/dist/public ./dist",
  "outputDirectory": "dist"
}
