import { useEffect } from "react";

/**
 * useThirdwebWalletSync — no-op hook kept for import compatibility.
 * Reown/WalletConnect has been removed; wagmi-to-ThirdWeb sync is no longer needed.
 */
export function useThirdwebWalletSync() {
  useEffect(() => {}, []);
}
